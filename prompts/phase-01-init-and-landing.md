Phase 1: Project Init + Beautiful Landing + One-Button Core.

Using the master context, initialize a Next.js 15 App Router + TS + Tailwind + shadcn/ui project (or confirm single-file heroic version if preferred — but recommend full for rich feed).

Implement:
- Clean landing page with hero, big delightful 'Hit me with something good' button.
- Button triggers a featured delight fetch (proxy one or two sources, e.g. joke + quote or NASA + animal).
- Display the delight beautifully in a card or modal.
- Basic navigation to 'Enter the Feed'.
- PWA manifest basics.
- Error/loading states that feel calm and premium.

Output: Full folder structure created, key files with code, and instructions for running locally. Test the button end-to-end with real API proxy.